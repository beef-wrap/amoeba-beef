{
  "name": "amoeba-beef",
  "scripts": {
    "build": "npx cmake-ts-gen@0.2.0",
    "clean": "npx --yes rimraf build node_modules && npm i"
  },
  "devDependencies": {
    "cmake-ts-gen": "0.2.0"
  }
}